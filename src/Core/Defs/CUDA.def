ifndef CUDA_DEF
CUDA_DEF=1

ifndef CUDA_HOME
  CUDA_HOME=/usr/local/cuda
endif

INCLUDES+= -I$(CUDA_HOME)/include

ifeq ($(ARC),linux)
  CUDA_LIB=$(CUDA_HOME)/lib
else
  CUDA_LIB=$(CUDA_HOME)/lib64
endif
 
EXELIB :=  -L$(CUDA_LIB) -lcuda -lcudart $(LIBPATHSWITCH)$(CUDA_LIB)   $(EXELIB)

endif
